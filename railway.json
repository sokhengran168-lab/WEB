{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
    "builder": "NIXPACKS",
    "installCommand": "npm ci --silent && composer install --no-interaction --optimize-autoloader",
    "buildCommand": "npm run build"
  },
  "deploy": {
    "startCommand": "php scripts/wait-for-db.php && php artisan migrate --force && php artisan storage:link && php artisan config:cache && php artisan route:cache && php artisan view:cache && php -S 0.0.0.0:$PORT -t public"
